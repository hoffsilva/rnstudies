import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    fontSize: 12,
    fontWeight: '600',
    flexDirection: 'row',
    alignItems: 'center',
    gap: 8,
  },
  title: {}
});